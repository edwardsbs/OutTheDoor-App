networks:
  shared-net:
    external: true

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: out-the-door
    restart: unless-stopped
    ports:
      - "${APP_PORT:-8081}:80"
    networks:
      - shared-net
